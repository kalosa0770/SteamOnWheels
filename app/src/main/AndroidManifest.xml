<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Steam on Wheels"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <!-- Launcher stays MainActivity — it already checks
             SessionManager.isLoggedIn() in onCreate() and redirects to
             LoginActivity (or TeacherDashboardActivity for teachers), so
             it works as the entry-point gatekeeper without needing to
             change the launcher itself. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LessonActivity"
            android:exported="false" />

        <activity
            android:name=".UploadLessonActivity"
            android:exported="false" />

        <!-- Pre-auth -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- Pupil -->
        <activity
            android:name=".AllLessonsActivity"
            android:exported="false" />

        <activity
            android:name=".SubjectLessonsActivity"
            android:exported="false" />

        <activity
            android:name=".ProgressActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".AccountSettingsActivity"
            android:exported="false" />

        <!-- Teacher -->
        <activity
            android:name=".TeacherDashboardActivity"
            android:exported="false" />

    </application>
</manifest>